import asyncio
class productivity:
    def __init__(self):
        return self
    def __init_subclass__(self):
        return self
class instill(productivity):
    async def __init__(self):
        super(instill, self).__init__()
async def intent():
    def __init__(self__):
        asyncio.create_task(instill)
outbound = asyncio.shield(intent())
inbound = asyncio.start_server(intent())
list(map(lambda: outbound+inbound, []))